bash ROWS=$(cat /home/claude/rows.json) cat > /home/claude/dashboard.html << 'HTMLEOF' <!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Attendance Dashboard — CFC3 Inventory</title> <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script> <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet"> <style> :root{--bg:#08101e;--surf:#0f1c2e;--surf2:#152035;--bord:#1e3050;--pres:#22d376;--abs:#f04e6a;--leave:#fbbf24;--text:#dbeafe;--muted:#5b7da8;--acc:#38bdf8;--acc2:#a78bfa;--off:#334155} *{box-sizing:border-box;margin:0;padding:0} body{background:var(--bg);color:var(--text);font-family:"Syne",sans-serif;min-height:100vh;padding-bottom:56px} /* HEADER */ header{background:linear-gradient(110deg,#060f1c 0%,#0d1f38 100%);border-bottom:1px solid var(--bord);padding:18px 32px;display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:200;box-shadow:0 4px 24px rgba(0,0,0,.4)} .htitle{font-size:20px;font-weight:800;letter-spacing:-.5px;background:linear-gradient(90deg,#38bdf8,#a78bfa);-webkit-background-clip:text;-webkit-text-fill-color:transparent} .hsub{font-size:11px;color:var(--muted);font-family:"DM Mono",monospace;margin-top:2px} .badges{display:flex;gap:8px} .bge{padding:3px 11px;border-radius:99px;font-size:11px;font-family:"DM Mono",monospace;font-weight:500;letter-spacing:.3px} .bge-b{background:rgba(56,189,248,.12);color:var(--acc);border:1px solid rgba(56,189,248,.25)} .bge-g{background:rgba(34,211,118,.12);color:var(--pres);border:1px solid rgba(34,211,118,.25)} /* MAIN */ .main{padding:22px 32px;max-width:1600px;margin:0 auto} /* FILTERS */ .filters{background:var(--surf);border:1px solid var(--bord);border-radius:14px;padding:16px 22px;display:flex;gap:14px;align-items:flex-end;flex-wrap:wrap;margin-bottom:20px;box-shadow:0 2px 16px rgba(0,0,0,.2)} .fg{display:flex;flex-direction:column;gap:5px} .fg label{font-size:10px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:1.1px;font-family:"DM Mono",monospace} .fg select,.fg input{background:var(--surf2);border:1px solid var(--bord);color:var(--text);border-radius:8px;padding:8px 12px;font-size:13px;font-family:"Syne",sans-serif;outline:none;cursor:pointer;transition:border-color .2s;min-width:148px;-webkit-appearance:none} .fg select:focus,.fg input:focus{border-color:var(--acc);box-shadow:0 0 0 3px rgba(56,189,248,.1)} .fg input::placeholder{color:var(--muted)} .btn-apply{padding:9px 22px;background:linear-gradient(135deg,#38bdf8,#818cf8);border:none;border-radius:8px;color:#fff;font-family:"Syne",sans-serif;font-weight:700;font-size:13px;cursor:pointer;transition:opacity .2s;align-self:flex-end;letter-spacing:.2px} .btn-apply:hover{opacity:.85} .btn-reset{padding:9px 16px;background:var(--surf2);border:1px solid var(--bord);border-radius:8px;color:var(--muted);font-family:"Syne",sans-serif;font-weight:600;font-size:13px;cursor:pointer;transition:all .2s;align-self:flex-end} .btn-reset:hover{border-color:var(--acc);color:var(--acc)} /* ALERT */ .alert-box{background:linear-gradient(120deg,rgba(240,78,106,.09),rgba(240,78,106,.03));border:1px solid rgba(240,78,106,.3);border-radius:12px;padding:11px 18px;margin-bottom:18px;display:flex;gap:10px;font-size:13px;color:#fdaab5;align-items:flex-start;line-height:1.5} .alert-box strong{color:#f04e6a} /* KPI GRID */ .kpi-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:12px;margin-bottom:20px} .kc{background:var(--surf);border:1px solid var(--bord);border-radius:14px;padding:16px 14px;position:relative;overflow:hidden;transition:transform .18s,box-shadow .18s;cursor:default} .kc:hover{transform:translateY(-2px);box-shadow:0 10px 32px rgba(0,0,0,.35)} .kc::after{content:"";position:absolute;top:0;left:0;right:0;height:3px;border-radius:14px 14px 0 0} .kc.k0::after{background:linear-gradient(90deg,#38bdf8,#818cf8)} .kc.k1::after{background:linear-gradient(90deg,#22d376,#16a34a)} .kc.k2::after{background:linear-gradient(90deg,#f04e6a,#dc2626)} .kc.k3::after{background:linear-gradient(90deg,#fbbf24,#d97706)} .kc.k4::after{background:linear-gradient(90deg,#a78bfa,#7c3aed)} .kc.k5::after{background:linear-gradient(90deg,#06b6d4,#0891b2)} .kc.k6::after{background:linear-gradient(90deg,#fb923c,#ea580c)} .klbl{font-size:9.5px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:1.1px;font-family:"DM Mono",monospace;margin-bottom:8px} .kval{font-size:26px;font-weight:800;line-height:1;margin-bottom:4px} .kc.k0 .kval{color:var(--acc)} .kc.k1 .kval{color:var(--pres)} .kc.k2 .kval{color:var(--abs)} .kc.k3 .kval{color:var(--leave)} .kc.k4 .kval{color:var(--acc2)} .kc.k5 .kval{color:#06b6d4} .kc.k6 .kval{color:#fb923c} .ksub{font-size:10px;color:var(--muted);font-family:"DM Mono",monospace} .kico{position:absolute;right:12px;top:50%;transform:translateY(-50%);font-size:24px;opacity:.1} /* CHART LAYOUTS */ .row2{display:grid;grid-template-columns:2fr 1fr;gap:18px;margin-bottom:18px} .row22{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-bottom:18px} .cc{background:var(--surf);border:1px solid var(--bord);border-radius:16px;padding:20px;box-shadow:0 2px 12px rgba(0,0,0,.15)} .ctitle{font-size:13px;font-weight:700;margin-bottom:3px;letter-spacing:-.1px} .csub{font-size:10.5px;color:var(--muted);font-family:"DM Mono",monospace;margin-bottom:14px} /* DONUT */ .donut-wrap{display:flex;gap:18px;align-items:center} .legend-list{display:flex;flex-direction:column;gap:8px} .leg-item{display:flex;align-items:center;gap:8px} .leg-dot{width:8px;height:8px;border-radius:2px;flex-shrink:0} .leg-lbl{font-size:11px;color:var(--muted);flex:1;white-space:nowrap} .leg-val{font-size:12px;font-weight:700;font-family:"DM Mono",monospace} /* MONTHLY CARDS */ .mcards{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:12px} .mc{background:var(--surf2);border-radius:10px;padding:12px;border:1px solid var(--bord)} .mc-name{font-size:9.5px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:1px;margin-bottom:6px} .mc-rate{font-size:20px;font-weight:800;margin-bottom:3px} .mc-det{font-size:10px;color:var(--muted);font-family:"DM Mono",monospace;line-height:1.7} /* RANK TABLES */ .rrow{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-bottom:18px} .tc{background:var(--surf);border:1px solid var(--bord);border-radius:16px;padding:20px;overflow:hidden} .rtable{width:100%;border-collapse:collapse;font-size:12px} .rtable th{font-size:9.5px;font-family:"DM Mono",monospace;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:.8px;padding:0 8px 9px;text-align:left;border-bottom:1px solid var(--bord)} .rtable td{padding:9px 8px;border-bottom:1px solid rgba(30,48,80,.7);vertical-align:middle} .rtable tr:last-child td{border-bottom:none} .rtable tr:hover td{background:rgba(56,189,248,.04)} .rank-n{font-family:"DM Mono",monospace;font-weight:800;font-size:11px;width:22px;height:22px;border-radius:6px;display:inline-flex;align-items:center;justify-content:center} .rg{background:rgba(251,191,36,.18);color:#fbbf24} .rs{background:rgba(148,163,184,.18);color:#94a3b8} .rb2{background:rgba(180,83,9,.18);color:#c2692b} .rn{background:rgba(91,125,168,.12);color:var(--muted)} .ename{font-weight:600;color:var(--text);max-width:150px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap} .eid{font-size:10px;color:var(--muted);font-family:"DM Mono",monospace;margin-top:1px} .sp{font-family:"DM Mono",monospace;font-size:10px;padding:2px 8px;border-radius:99px} .sm{background:rgba(56,189,248,.13);color:var(--acc)} .sn{background:rgba(167,139,250,.13);color:var(--acc2)} .sb{background:rgba(251,191,36,.13);color:#fbbf24} .rbar{display:flex;align-items:center;gap:7px} .rtrack{flex:1;height:4px;background:var(--bord);border-radius:3px;overflow:hidden} .rfill{height:100%;border-radius:3px;transition:width .5s} .rtext{font-family:"DM Mono",monospace;font-size:11px;font-weight:700;white-space:nowrap} .rh{color:var(--pres)} .rm{color:var(--leave)} .rl{color:var(--abs)} /* DRILL */ .drill{background:var(--surf);border:1px solid var(--bord);border-radius:16px;padding:20px;margin-bottom:18px} .dhead{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:10px;margin-bottom:14px} .dsearch{background:var(--surf2);border:1px solid var(--bord);color:var(--text);border-radius:8px;padding:8px 14px;font-size:13px;font-family:"Syne",sans-serif;outline:none;width:220px;transition:border-color .2s} .dsearch:focus{border-color:var(--acc)} .dsearch::placeholder{color:var(--muted)} .dtable{width:100%;border-collapse:collapse;font-size:12px} .dtable th{font-size:9.5px;font-family:"DM Mono",monospace;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:.8px;padding:0 10px 9px;text-align:left;border-bottom:1px solid var(--bord);cursor:pointer;user-select:none;white-space:nowrap;transition:color .15s} .dtable th:hover{color:var(--acc)} .dtable td{padding:9px 10px;border-bottom:1px solid rgba(30,48,80,.5);vertical-align:middle} .dtable tr:last-child td{border-bottom:none} .dtable tr:hover td{background:rgba(56,189,248,.035)} .pill{display:inline-block;padding:2px 9px;border-radius:99px;font-family:"DM Mono",monospace;font-size:9.5px;font-weight:700;letter-spacing:.3px} .p-ex{background:rgba(34,211,118,.14);color:var(--pres)} .p-gd{background:rgba(56,189,248,.14);color:var(--acc)} .p-fa{background:rgba(251,191,36,.14);color:var(--leave)} .p-at{background:rgba(240,78,106,.14);color:var(--abs)} .pgn{display:flex;align-items:center;gap:7px;margin-top:14px;justify-content:center;flex-wrap:wrap} .pbtn{padding:5px 13px;background:var(--surf2);border:1px solid var(--bord);border-radius:7px;color:var(--muted);font-size:11px;font-family:"DM Mono",monospace;cursor:pointer;transition:all .18s} .pbtn:hover,.pbtn.act{border-color:var(--acc);color:var(--acc);background:rgba(56,189,248,.07)} .pinfo{font-size:11px;color:var(--muted);font-family:"DM Mono",monospace} .section-hd{display:flex;align-items:center;gap:9px;margin-bottom:14px} .sdot{width:7px;height:7px;border-radius:2px;flex-shrink:0} .stitle{font-size:14px;font-weight:700} .ssub{font-size:10px;color:var(--muted);font-family:"DM Mono",monospace} /* SORT INDICATOR */ th.sort-asc::after{content:" ▲";opacity:.7} th.sort-desc::after{content:" ▼";opacity:.7} ::-webkit-scrollbar{width:5px;height:5px} ::-webkit-scrollbar-track{background:var(--surf)} ::-webkit-scrollbar-thumb{background:var(--bord);border-radius:3px} @media(max-width:1200px){.kpi-grid{grid-template-columns:repeat(4,1fr)}.row2,.row22,.rrow{grid-template-columns:1fr}} </style> </head> <body> <header> <div> <div class="htitle">📊 Attendance Performance Dashboard</div> <div class="hsub">CFC3 · Inventory Team · Jan–Apr 2026 · 9,692 Records</div> </div> <div class="badges"> <span class="bge bge-b">CFC3 HUB</span> <span class="bge bge-g" id="liveTag">● ALL DATA</span> </div> </header> <div class="main"> <!-- FILTERS --> <div class="filters"> <div class="fg"><label>Month</label> <select id="fMonth"> <option value="0">All Months</option> <option value="1">January</option> <option value="2">February</option> <option value="3">March</option> <option value="4">April</option> </select> </div> <div class="fg"><label>Week</label> <select id="fWeek"><option value="0">All Weeks</option></select> </div> <div class="fg"><label>Shift</label> <select id="fShift"> <option value="">All Shifts</option> <option value="Morning">Morning</option> <option value="Night">Night</option> <option value="Between">Between</option> </select> </div> <div class="fg"><label>Status</label> <select id="fStatus"> <option value="">All Statuses</option> <option value="P">Present</option> <option value="A">Absent</option> <option value="LEAVE">On Leave (AL/SL/V…)</option> <option value="OFF">Day Off / Holiday</option> </select> </div> <div class="fg"><label>Employee ID / Name</label> <input type="text" id="fEmp" placeholder="Type to search…"> </div> <button class="btn-apply" onclick="applyFilters()">⚡ Apply Filters</button> <button class="btn-reset" onclick="resetFilters()">↺ Reset</button> </div> <!-- ALERT --> <div class="alert-box" id="alertBox"> <span style="font-size:16px;flex-shrink:0">⚠️</span> <span id="alertMsg"><strong>Attendance Alert:</strong> Loading data…</span> </div> <!-- KPIs --> <div class="kpi-grid"> <div class="kc k0"><div class="klbl">Total Employees</div><div class="kval" id="kEmp">—</div><div class="ksub">Unique headcount</div><span class="kico">👥</span></div> <div class="kc k1"><div class="klbl">Present Days</div><div class="kval" id="kPres">—</div><div class="ksub">Total check-ins</div><span class="kico">✅</span></div> <div class="kc k2"><div class="klbl">Absent Days</div><div class="kval" id="kAbs">—</div><div class="ksub">Unexcused</div><span class="kico">❌</span></div> <div class="kc k3"><div class="klbl">Leave Days</div><div class="kval" id="kLeave">—</div><div class="ksub">All leave types</div><span class="kico">🏖️</span></div> <div class="kc k4"><div class="klbl">Week-Offs</div><div class="kval" id="kWO">—</div><div class="ksub">Scheduled off</div><span class="kico">📅</span></div> <div class="kc k5"><div class="klbl">Attendance Rate</div><div class="kval" id="kRate">—</div><div class="ksub">Present / work days</div><span class="kico">📈</span></div> <div class="kc k6"><div class="klbl">Absenteeism Rate</div><div class="kval" id="kAbsR">—</div><div class="ksub">Absent / work days</div><span class="kico">📉</span></div> </div> <!-- Charts row 1 --> <div class="row2"> <div class="cc"> <div class="ctitle">Weekly Attendance Trend</div> <div class="csub">Attendance rate % & present/absent counts per week</div> <div style="position:relative;height:260px"><canvas id="weekChart"></canvas></div> </div> <div class="cc"> <div class="ctitle">Status Distribution</div> <div class="csub">Breakdown of all attendance records</div> <div class="donut-wrap"> <div style="width:175px;height:175px;flex-shrink:0"><canvas id="donutChart"></canvas></div> <div class="legend-list" id="donutLeg"></div> </div> </div> </div> <!-- Charts row 2 --> <div class="row22"> <div class="cc"> <div class="ctitle">Monthly Performance</div> <div class="csub">Present vs Leave vs Absent by month</div> <div style="position:relative;height:200px"><canvas id="monthChart"></canvas></div> <div class="mcards" id="monthCards"></div> </div> <div class="cc"> <div class="ctitle">Shift Distribution</div> <div class="csub">Records by assigned shift</div> <div style="position:relative;height:200px"><canvas id="shiftChart"></canvas></div> <div style="margin-top:14px;display:flex;gap:10px;flex-wrap:wrap" id="shiftTotals"></div> </div> </div> <!-- Top / Bottom --> <div class="rrow"> <div class="tc"> <div class="section-hd"> <div class="sdot" style="background:var(--pres)"></div> <div><div class="stitle">🏆 Top Performers</div><div class="ssub">Highest attendance rate (≥10 working days)</div></div> </div> <table class="rtable"><thead><tr><th>#</th><th>Employee</th><th>Shift</th><th>Present</th><th>Rate</th></tr></thead> <tbody id="topBody"></tbody></table> </div> <div class="tc"> <div class="section-hd"> <div class="sdot" style="background:var(--abs)"></div> <div><div class="stitle">⚠️ Frequent Absences</div><div class="ssub">Lowest attendance rate (≥5 working days)</div></div> </div> <table class="rtable"><thead><tr><th>#</th><th>Employee</th><th>Shift</th><th>Absent</th><th>Rate</th></tr></thead> <tbody id="botBody"></tbody></table> </div> </div> <!-- Drill --> <div class="drill"> <div class="dhead"> <div> <div class="stitle" style="font-size:15px">🔍 Employee Drill-Down</div> <div class="ssub" style="margin-top:2px">Click headers to sort · Shows filtered results</div> </div> <input class="dsearch" id="drillSearch" placeholder="🔍 Search name or ID…" oninput="renderDrill()"> </div> <div style="overflow-x:auto"> <table class="dtable"> <thead><tr> <th onclick="sortDrill('name')" id="th-name">Employee</th> <th onclick="sortDrill('shift')" id="th-shift">Shift</th> <th onclick="sortDrill('present')" id="th-present">Present</th> <th onclick="sortDrill('absent')" id="th-absent">Absent</th> <th onclick="sortDrill('leave')" id="th-leave">Leave</th> <th onclick="sortDrill('total')" id="th-total">Working Days</th> <th onclick="sortDrill('rate')" id="th-rate">Rate</th> <th>Status</th> </tr></thead> <tbody id="drillBody"></tbody> </table> </div> <div class="pgn" id="pgn"></div> </div> </div> <script> // ─── RAW DATA ──────────────────────────────────────────────────────────────── HTMLEOF echo "const ALL_ROWS = " >> /home/claude/dashboard.html cat /home/claude/rows.json >> /home/claude/dashboard.html echo ";" >> /home/claude/dashboard.html cat >> /home/claude/dashboard.html << 'SCRIPTEOF' const LEAVE_ST = new Set(['AL','SL','ALC','V','PL','CTL','DL']); const OFF_ST = new Set(['WO','PH','WPH','WOC']); const COLORS = ['#22d376','#f04e6a','#fbbf24','#818cf8','#38bdf8','#a78bfa','#fb923c','#34d399']; const MONTH_NM = {1:'January',2:'February',3:'March',4:'April'}; // Populate week dropdown (()=>{ const sel = document.getElementById('fWeek'); for(let w=1;w<=17;w++) sel.innerHTML += <option value="${w}">Week ${w}</option>; })(); // ─── FILTER STATE ──────────────────────────────────────────────────────────── let filtered = ALL_ROWS; function getFilters(){ return { month: +document.getElementById('fMonth').value, week: +document.getElementById('fWeek').value, shift: document.getElementById('fShift').value, status: document.getElementById('fStatus').value, emp: document.getElementById('fEmp').value.trim().toLowerCase() }; } function applyFilters(){ const f = getFilters(); filtered = ALL_ROWS.filter(r=>{ if(f.month && r.month !== f.month) return false; if(f.week && r.week !== f.week) return false; if(f.shift && r.shift !== f.shift) return false; if(f.status){ if(f.status==='LEAVE' && !LEAVE_ST.has(r.status)) return false; else if(f.status==='OFF' && !OFF_ST.has(r.status)) return false; else if(f.status!=='LEAVE' && f.status!=='OFF' && r.status!==f.status) return false; } if(f.emp && !r.name.toLowerCase().includes(f.emp) && !r.id.toLowerCase().includes(f.emp)) return false; return true; }); const label = buildLabel(f); document.getElementById('liveTag').textContent = label ? ● ${label} : '● ALL DATA'; updateAll(); } function buildLabel(f){ const parts=[]; if(f.month) parts.push(MONTH_NM[f.month]); if(f.week) parts.push(W${f.week}); if(f.shift) parts.push(f.shift); if(f.status) parts.push(f.status==='LEAVE'?'On Leave':f.status==='OFF'?'Day Off':f.status); if(f.emp) parts.push("${f.emp}"); return parts.join(' · '); } function resetFilters(){ ['fMonth','fWeek','fShift','fStatus'].forEach(id=>document.getElementById(id).value=id==='fMonth'||id==='fWeek'?'0':''); document.getElementById('fEmp').value=''; filtered = ALL_ROWS; document.getElementById('liveTag').textContent='● ALL DATA'; updateAll(); } // ─── COMPUTE STATS ─────────────────────────────────────────────────────────── function computeKPI(rows){ const empSet = new Set(rows.map(r=>r.id)); const work = rows.filter(r=>r.isWork); const pres = rows.filter(r=>r.status==='P').length; const abs = rows.filter(r=>r.status==='A').length; const leave = rows.filter(r=>LEAVE_ST.has(r.status)).length; const wo = rows.filter(r=>OFF_ST.has(r.status)).length; const rate = work.length ? (pres/work.length*100).toFixed(1) : 0; const arate = work.length ? (abs/work.length*100).toFixed(1) : 0; return {emp:empSet.size, pres, abs, leave, wo, rate, arate, workDays:work.length}; } function computeWeekly(rows){ const map={}; rows.forEach(r=>{ if(!map[r.week]) map[r.week]={week:r.week,pres:0,abs:0,work:0}; if(r.status==='P') map[r.week].pres++; if(r.status==='A') map[r.week].abs++; if(r.isWork) map[r.week].work++; }); return Object.values(map).sort((a,b)=>a.week-b.week); } function computeMonthly(rows){ const map={}; rows.forEach(r=>{ if(!map[r.month]) map[r.month]={m:r.month,pres:0,abs:0,leave:0,work:0}; if(r.status==='P') map[r.month].pres++; if(r.status==='A') map[r.month].abs++; if(LEAVE_ST.has(r.status)) map[r.month].leave++; if(r.isWork) map[r.month].work++; }); return Object.values(map).sort((a,b)=>a.m-b.m); } function computeStatus(rows){ const map={}; rows.forEach(r=>{ const lbl = r.status==='P'?'Present':r.status==='A'?'Absent': LEAVE_ST.has(r.status)?r.status+' (Leave)':OFF_ST.has(r.status)?'Day Off':r.status; map[lbl]=(map[lbl]||0)+1; }); return map; } function computeShift(rows){ const map={Morning:0,Night:0,Between:0}; rows.forEach(r=>{ if(map[r.shift]!==undefined) map[r.shift]++; }); return map; } function computeEmpStats(rows){ const map={}; rows.forEach(r=>{ if(!map[r.id]) map[r.id]={id:r.id,name:r.name,shift:r.shift,pres:0,abs:0,leave:0,work:0}; if(r.status==='P') map[r.id].pres++; if(r.status==='A') map[r.id].abs++; if(LEAVE_ST.has(r.status)) map[r.id].leave++; if(r.isWork) map[r.id].work++; }); return Object.values(map).map(e=>({...e,rate:e.work?+(e.pres/e.work*100).toFixed(1):0})); } // ─── CHARTS ────────────────────────────────────────────────────────────────── Chart.defaults.color='#5b7da8'; Chart.defaults.borderColor='#1e3050'; Chart.defaults.font.family="'DM Mono',monospace"; let weekChart, donutChart, monthChart, shiftChart; function initCharts(){ weekChart = new Chart(document.getElementById('weekChart'),{ type:'line', data:{labels:[],datasets:[ {label:'Rate %',data:[],borderColor:'#38bdf8',backgroundColor:'rgba(56,189,248,.07)',tension:.4,fill:true,pointRadius:4,pointBackgroundColor:'#38bdf8',pointBorderColor:'#08101e',pointBorderWidth:2,yAxisID:'y'}, {label:'Present',data:[],borderColor:'#22d376',backgroundColor:'transparent',tension:.4,pointRadius:3,yAxisID:'y1'}, {label:'Absent',data:[],borderColor:'#f04e6a',backgroundColor:'transparent',tension:.4,pointRadius:3,yAxisID:'y1'} ]}, options:{responsive:true,maintainAspectRatio:false, interaction:{mode:'index',intersect:false}, plugins:{legend:{labels:{boxWidth:10,padding:14,font:{size:10}}},tooltip:{backgroundColor:'#0f1c2e',borderColor:'#1e3050',borderWidth:1,padding:10}}, scales:{ x:{grid:{color:'rgba(30,48,80,.6)'},ticks:{font:{size:9}}}, y:{type:'linear',position:'left',min:60,max:100,grid:{color:'rgba(30,48,80,.6)'},ticks:{callback:v=>v+'%',font:{size:9}}}, y1:{type:'linear',position:'right',grid:{display:false},ticks:{font:{size:9}}} }} }); donutChart = new Chart(document.getElementById('donutChart'),{ type:'doughnut', data:{labels:[],datasets:[{data:[],backgroundColor:COLORS,borderWidth:0,hoverOffset:8}]}, options:{responsive:true,maintainAspectRatio:false,cutout:'70%', plugins:{legend:{display:false},tooltip:{backgroundColor:'#0f1c2e',borderColor:'#1e3050',borderWidth:1,padding:10}}} }); monthChart = new Chart(document.getElementById('monthChart'),{ type:'bar', data:{labels:[],datasets:[ {label:'Present',data:[],backgroundColor:'rgba(34,211,118,.75)',borderRadius:6,barThickness:24}, {label:'Leave',data:[],backgroundColor:'rgba(251,191,36,.75)',borderRadius:6,barThickness:24}, {label:'Absent',data:[],backgroundColor:'rgba(240,78,106,.75)',borderRadius:6,barThickness:24} ]}, options:{responsive:true,maintainAspectRatio:false, plugins:{legend:{labels:{boxWidth:10,padding:12,font:{size:10}}},tooltip:{backgroundColor:'#0f1c2e',borderColor:'#1e3050',borderWidth:1,padding:10}}, scales:{x:{grid:{display:false},ticks:{font:{size:10}}},y:{grid:{color:'rgba(30,48,80,.6)'},ticks:{font:{size:9}}}}} }); shiftChart = new Chart(document.getElementById('shiftChart'),{ type:'bar', data:{labels:['Morning','Night','Between'],datasets:[{data:[0,0,0],backgroundColor:['rgba(56,189,248,.8)','rgba(167,139,250,.8)','rgba(251,191,36,.8)'],borderRadius:8,barThickness:42}]}, options:{responsive:true,maintainAspectRatio:false,indexAxis:'y', plugins:{legend:{display:false},tooltip:{backgroundColor:'#0f1c2e',borderColor:'#1e3050',borderWidth:1,padding:10}}, scales:{x:{grid:{color:'rgba(30,48,80,.6)'},ticks:{font:{size:9}}},y:{grid:{display:false},ticks:{font:{size:12,weight:'600'}}}}} }); } // ─── UPDATE ALL ─────────────────────────────────────────────────────────────── function updateAll(){ const rows = filtered; const kpi = computeKPI(rows); // KPIs document.getElementById('kEmp').textContent = kpi.emp.toLocaleString(); document.getElementById('kPres').textContent = kpi.pres.toLocaleString(); document.getElementById('kAbs').textContent = kpi.abs.toLocaleString(); document.getElementById('kLeave').textContent = kpi.leave.toLocaleString(); document.getElementById('kWO').textContent = kpi.wo.toLocaleString(); document.getElementById('kRate').textContent = kpi.rate+'%'; document.getElementById('kAbsR').textContent = kpi.arate+'%'; // Alert const empStats = computeEmpStats(rows); const atRisk = empStats.filter(e=>e.work>=5&&e.rate<70).length; const highAbs = empStats.filter(e=>e.abs>=8); const worstWeek = computeWeekly(rows).sort((a,b)=>b.abs-a.abs)[0]; document.getElementById('alertMsg').innerHTML = <strong>Attendance Alert:</strong> ${kpi.arate}% absenteeism rate in current view. + <strong>${atRisk} employee${atRisk!==1?'s':''}</strong> below 70% attendance. + (worstWeek ? Week <strong>${worstWeek.week}</strong> has the most absences (${worstWeek.abs}). : '') + (highAbs.length ? <strong>${highAbs.length}</strong> employee${highAbs.length!==1?'s':''} with 8+ absent days. : ''); // Weekly chart const wk = computeWeekly(rows); weekChart.data.labels = wk.map(w=>W${w.week}); weekChart.data.datasets[0].data = wk.map(w=>w.work?+(w.pres/w.work*100).toFixed(1):0); weekChart.data.datasets[1].data = wk.map(w=>w.pres); weekChart.data.datasets[2].data = wk.map(w=>w.abs); // Dynamic y axis min const rates = weekChart.data.datasets[0].data.filter(v=>v>0); weekChart.options.scales.y.min = rates.length ? Math.max(0, Math.floor(Math.min(...rates)-5)) : 0; weekChart.update(); // Donut const sd = computeStatus(rows); const sdKeys = Object.keys(sd).sort((a,b)=>sd[b]-sd[a]); const sdVals = sdKeys.map(k=>sd[k]); const sdTotal = sdVals.reduce((a,b)=>a+b,0); donutChart.data.labels = sdKeys; donutChart.data.datasets[0].data = sdVals; donutChart.data.datasets[0].backgroundColor = COLORS.slice(0,sdKeys.length); donutChart.update(); const leg = document.getElementById('donutLeg'); leg.innerHTML = sdKeys.map((k,i)=> <div class="leg-item"> <div class="leg-dot" style="background:${COLORS[i%COLORS.length]}"></div> <span class="leg-lbl">${k}</span> <span class="leg-val" style="color:${COLORS[i%COLORS.length]}">${sd[k].toLocaleString()}</span> </div>).join(''); // Monthly chart const mo = computeMonthly(rows); monthChart.data.labels = mo.map(m=>MONTH_NM[m.m]||M${m.m}); monthChart.data.datasets[0].data = mo.map(m=>m.pres); monthChart.data.datasets[1].data = mo.map(m=>m.leave); monthChart.data.datasets[2].data = mo.map(m=>m.abs); monthChart.update(); const mc = document.getElementById('monthCards'); mc.innerHTML = mo.map(m=>{ const rate = m.work ? (m.pres/m.work*100).toFixed(1) : 0; const c = rate>=87?'var(--pres)':rate>=82?'var(--leave)':'var(--abs)'; return <div class="mc"><div class="mc-name">${MONTH_NM[m.m]||M${m.m}}</div> <div class="mc-rate" style="color:${c}">${rate}%</div> <div class="mc-det">✅ ${m.pres.toLocaleString()} present<br>❌ ${m.abs} absent<br>🏖️ ${m.leave} leave</div></div>; }).join(''); // Shift chart const sh = computeShift(rows); shiftChart.data.datasets[0].data = [sh.Morning, sh.Night, sh.Between]; shiftChart.update(); document.getElementById('shiftTotals').innerHTML = [ ['Morning',sh.Morning,'var(--acc)'],['Night',sh.Night,'var(--acc2)'],['Between',sh.Between,'var(--leave)'] ].map(([l,v,c])=><span style="font-family:'DM Mono',monospace;font-size:11px;padding:4px 10px;background:var(--surf2);border:1px solid var(--bord);border-radius:6px"> <span style="color:${c};font-weight:700">${l}</span> <span style="color:var(--muted)">${v.toLocaleString()}</span></span>).join(''); // Top / Bottom const top = empStats.filter(e=>e.work>=10).sort((a,b)=>b.rate-a.rate||b.pres-a.pres).slice(0,8); const bot = empStats.filter(e=>e.work>=5).sort((a,b)=>a.rate-b.rate||b.abs-a.abs).slice(0,8); document.getElementById('topBody').innerHTML = top.map((e,i)=>empRow(e,i,'top')).join(''); document.getElementById('botBody').innerHTML = bot.map((e,i)=>empRow(e,i,'bot')).join(''); // Drill drillPage=0; renderDrill(); } // ─── HELPERS ───────────────────────────────────────────────────────────────── function shiftBadge(s){ return <span class="sp ${s==='Morning'?'sm':s==='Night'?'sn':'sb'}">${s}</span>; } function rankBadge(i){ const c=['rg','rs','rb2','rn'][Math.min(i,3)]; return <span class="rank-n ${c}">${i+1}</span>; } function rateBar(r){ const c=r>=90?'var(--pres)':r>=80?'var(--leave)':'var(--abs)'; const tc=r>=90?'rh':r>=80?'rm':'rl'; return <div class="rbar"><div class="rtrack"><div class="rfill" style="width:${r}%;background:${c}"></div></div><span class="rtext ${tc}">${r}%</span></div>; } function empRow(e,i,type){ return <tr><td>${rankBadge(i)}</td> <td><div class="ename">${e.name}</div><div class="eid">${e.id}</div></td> <td>${shiftBadge(e.shift)}</td> <td><span style="font-family:'DM Mono',monospace;color:${type==='top'?'var(--pres)':'var(--abs)'};font-weight:700">${type==='top'?e.pres:e.abs}</span></td> <td>${rateBar(e.rate)}</td></tr>; } function statusPill(r){ if(r>=90) return '<span class="pill p-ex">Excellent</span>'; if(r>=80) return '<span class="pill p-gd">Good</span>'; if(r>=70) return '<span class="pill p-fa">Fair</span>'; return '<span class="pill p-at">At Risk</span>'; } // ─── DRILL DOWN ─────────────────────────────────────────────────────────────── let drillPage=0, drillKey='rate', drillDir=-1; const PAGE=12; function sortDrill(key){ document.querySelectorAll('.dtable th').forEach(th=>{th.classList.remove('sort-asc','sort-desc')}); if(drillKey===key) drillDir*=-1; else{drillKey=key;drillDir=-1;} const th = document.getElementById('th-'+key); if(th) th.classList.add(drillDir===-1?'sort-desc':'sort-asc'); drillPage=0; renderDrill(); } function renderDrill(){ const search = document.getElementById('drillSearch').value.toLowerCase(); const empStats = computeEmpStats(filtered); let data = empStats.filter(e=> !search || e.name.toLowerCase().includes(search) || e.id.toLowerCase().includes(search)); data.sort((a,b)=>{ const va=drillKey==='name'?a.name:a[drillKey]; const vb=drillKey==='name'?b.name:b[drillKey]; return typeof va==='string'?drillDir*va.localeCompare(vb):drillDir*(va-vb); }); const total=data.length, pages=Math.ceil(total/PAGE)||1; if(drillPage>=pages) drillPage=0; const slice=data.slice(drillPage*PAGE,(drillPage+1)*PAGE); document.getElementById('drillBody').innerHTML = slice.map(e=> <tr> <td><div class="ename" style="max-width:190px">${e.name}</div><div class="eid">${e.id}</div></td> <td>${shiftBadge(e.shift)}</td> <td style="font-family:'DM Mono',monospace;color:var(--pres);font-weight:600">${e.pres}</td> <td style="font-family:'DM Mono',monospace;color:${e.abs>=8?'var(--abs)':e.abs>=3?'var(--leave)':'var(--muted)'};font-weight:600">${e.abs}</td> <td style="font-family:'DM Mono',monospace;color:var(--leave);font-weight:600">${e.leave}</td> <td style="font-family:'DM Mono',monospace;color:var(--muted)">${e.work}</td> <td>${rateBar(e.rate)}</td> <td>${statusPill(e.rate)}</td> </tr>).join(''); const pg=document.getElementById('pgn'); pg.innerHTML = total>PAGE ? <span class="pinfo">${drillPage*PAGE+1}–${Math.min((drillPage+1)*PAGE,total)} of ${total}</span> + Array.from({length:pages},(_,p)=><button class="pbtn${p===drillPage?' act':''}" onclick="drillPage=${p};renderDrill()">${p+1}</button>).join('') : ''; } // ─── BOOT ──────────────────────────────────────────────────────────────────── initCharts(); updateAll(); </script> </body> </html> SCRIPTEOF echo "Done. Size: $(wc -c < /home/claude/dashboard.html) bytes" Output Done. Size: 1103204 bytes
